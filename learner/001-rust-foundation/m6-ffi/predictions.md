# Learner Track — Module 6 预测表

**Story**: US6 | **Capabilities**: C-21

## 规则

1. `我的预测` 与 `依据` 两列 MUST 在运行**任何**验证命令之前填写并**提交**。
2. 提交之后才允许跑 `验证命令`，然后回填 `实测` 与 `一致?`。
3. 事后回填的预测**无效**，该行判未完成。
4. `依据` 写不出来就填"暂无依据" —— 那同样是有效信息。
   写"感觉是"或"应该吧"则等于没填。

> `一致? = ✗` **不是失败**。它精确定位了一处心智模型缺口。
> 反过来，全中才值得怀疑 —— 检查一下是不是事后回填的。

## 预测表

| # | C-ID | 预测项 | 我的预测 | 依据（为什么） | 验证命令 | 实测 | 一致? |
|---|------|-------|---------|--------------|---------|------|-------|
| 1 | C-21 | `#[repr(C)]` 结构体与 C 侧同名结构体：`size_of` / `align_of` / 三个字段的 `offset_of` **两侧是否一致**（先写是或否，不要填具体字节数） | | | `cargo test -p m6-ffi --test c21_ffi_layout`；`cargo run -p m6-ffi --example c21_ffi_layout` | | |
| 2 | C-21 | Rust→C：C 侧加法对一对小整数返回什么 | | | `cargo test -p m6-ffi --test c21_ffi`；`cargo run -p m6-ffi --example c21_ffi` | | |
| 3 | C-21 | C→Rust：C 包装去调 Rust 导出的乘法，返回什么 | | | 同上（`c21_ffi` 里 C→Rust 那条断言） | | |
| 4 | C-21 | C `malloc` 一块字符串、约定由 Rust `free`：读回的内容是否等于约定字面量；释放责任是否按约定执行 | | | `cargo test -p m6-ffi --test c21_ffi_ownership`；`cargo run -p m6-ffi --example c21_ffi_ownership` | | |
| 5 | C-21 | `libc::open` 一个不存在的路径，封装成 Rust 错误类型之后，**原始整数错误码是否还在**（会不会被吃掉只剩一句空话） | | | `cargo test -p m6-ffi --test c21_errno`；`cargo run -p m6-ffi --example c21_errno` | | |
| 6 | C-21 | 本模块判定脚本跑完：退出码是否为 0；若为 0，你把结论写成"未观测到内存错误"还是"无 UB" | | | `tools/run-asan.sh m6-ffi` | | |

## 未命中复盘                   <!-- 仅当存在 `一致? = ✗` -->

每条不一致写三段。不要只写"记错了"——要定位到心智模型的哪一步出了偏差。

### #N（C-21）

- **我原本以为**：
- **实际是**：
- **我的心智模型错在哪一步**：

## 打开 Answer Track 的记录       <!-- 仅当翻过答案 -->

| 时间 | 翻的是哪份 | 触发条件（§H4.1 的第几条） | 当时的卡点 |
|------|-----------|------------------------|-----------|
| | | | |
