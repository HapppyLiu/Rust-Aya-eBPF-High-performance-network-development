# Learner Track — Module 8 预测表

**Story**: US8 | **Capabilities**: C-01…C-24

## 规则

1. `我的预测` 与 `依据` 两列 MUST 在运行**任何**验证命令之前填写并**提交**。
2. 提交之后才允许跑 `验证命令`，然后回填 `实测` 与 `一致?`。
3. 事后回填的预测**无效**，该行判未完成。
4. `依据` 写不出来就填"暂无依据" —— 那同样是有效信息。
   写"感觉是"或"应该吧"则等于没填。
5. 本表**不**提供设计方案。预测的是你自己的设计在命令下会看到什么，
   不是去猜 Answer Track 里写了哪个函数名。

> `一致? = ✗` **不是失败**。它精确定位了一处心智模型缺口。
> 反过来，全中才值得怀疑 —— 检查一下是不是事后回填的。

## 预测表

| # | C-ID | 预测项 | 我的预测 | 依据（为什么） | 验证命令 | 实测 | 一致? |
|---|------|-------|---------|--------------|---------|------|-------|
| 1 | C-01…C-04 | 解析函数返回的载荷，是原字节缓冲的子切片，还是新分配的拷贝？（写"子切片 / 拷贝 / 取决于某条件"） | | | `cargo test -p m8-capstone --test capstone` | | |
| 2 | C-01…C-04 | 拥有缓冲的类型被移走之后，原先拿到的视图还能不能继续用？预测编译期还是运行期给出答案 | | | 同上；必要时自己写一段会否编译的草稿 | | |
| 3 | C-05…C-07 | 未知报文种类：成功、还是错误枚举里的哪一类失败（用你自己的错误名，不要抄答案） | | | `cargo test -p m8-capstone --test capstone` | | |
| 4 | C-08 | 切片比报文头还短：走哪条错误路径？`?` 会不会再包一层别的错误类型？ | | | 同上 | | |
| 5 | C-09…C-11 | 空缓冲上的帧迭代：立即结束，还是先产出一次失败？`Box<dyn …>` 解析一次会不会因此多一次堆分配？ | | | 同上（分配次数用 `measure`，先写你预测的次数，不要看测试源码） | | |
| 6 | C-12…C-14 | 只含原子计数的统计类型：`Send`？`Sync`？你有没有自己写 `unsafe impl`？（是/否） | | | 同上里的静态断言测试 | | |
| 7 | C-15…C-20 | 公开 API 传入过短切片：返回错误，还是读穿？ | | | `cargo test -p m8-capstone --test raw` | | |
| 8 | C-15…C-20 | 对综合实验的**安全路径**跑 Miri：事前把判定档写成哪一档（工具未报告问题 / 尚未运行 / 预期中的问题）。未跑之前能不能填"未报告问题"？ | | | `cargo +nightly miri test -p m8-capstone` | | |
| 9 | C-21 | `repr(C)` 报文头：`size_of` / `align_of` / 最后一个字段的 `offset_of`（三个数字你自己按字段列出来） | | | `cargo test -p m8-capstone --test wire` | | |
| 10 | C-22…C-24 | `cargo build -p m8-capstone --no-default-features` 是否成功？成功证明了什么、**不能**证明什么？ | | | `cargo build -p m8-capstone --no-default-features` | | |
| 11 | C-22…C-24 | 只解析一段已经在栈上的借用视图（不 `Box`、不 `Vec`）：`measure` 看到的 `allocs` 是几次？ | | | `cargo test -p m8-capstone --test capstone` | | |
| 12 | 全部 | 24 项能力能否各自填到**一个文件 + 一个函数**？你觉得哪几项最可能只能写进"配套说明"而不是源码？ | | | 对照 `acceptance/capability-location-map.md`（写完自己的表之后再打开） | | |

## 未命中复盘                   <!-- 仅当存在 `一致? = ✗` -->

每条不一致写三段。不要只写"记错了"——要定位到心智模型的哪一步出了偏差。

### #N（C-xx）

- **我原本以为**：
- **实际是**：
- **我的心智模型错在哪一步**：

## 打开 Answer Track 的记录       <!-- 仅当翻过答案 -->

| 时间 | 翻的是哪份 | 触发条件（§H4.1 的第几条） | 当时的卡点 |
|------|-----------|------------------------|-----------|
| | | | |
