# Learner Track — Module 1 预测表

**Story**: US1 | **Capabilities**: C-01…C-04

## 规则

1. `我的预测` 与 `依据` 两列 MUST 在运行**任何**验证命令之前填写并**提交**。
2. 提交之后才允许跑 `验证命令`，然后回填 `实测` 与 `一致?`。
3. 事后回填的预测**无效**，该行判未完成。
4. `依据` 写不出来就填"暂无依据" —— 那同样是有效信息，说明这一项你还没形成判断。
   写"感觉是"或"应该吧"则等于没填。

> `一致? = ✗` **不是失败**。它精确定位了一处心智模型缺口，是本模块最有价值的产出。
> 反过来，10 项全中才值得怀疑 —— 检查一下是不是事后回填的。

## 预测表

| # | C-ID | 预测项 | 我的预测 | 依据（为什么） | 验证命令 | 实测 | 一致? |
|---|------|-------|---------|--------------|---------|------|-------|
| 1 | C-01 | 同一作用域内声明 a、b、c 三个值，销毁顺序是什么 | | | `cargo test -p m1-ownership --test c01_ownership` | | |
| 2 | C-01 | 把一个值传进函数（取得所有权），它在**调用点之后**还是**函数返回前**被销毁 | | | 同上 | | |
| 3 | C-01 | 一个值被移动给新变量后，销毁**总次数**是 1 还是 2 | | | 同上 | | |
| 4 | C-02 | 对"赋值即复制"的类型做 `let b = a;` 后，`a` 还能不能用 | | | `cargo test -p m1-ownership --test c02_move` | | |
| 5 | C-02 | 把值从原位置"取走并留下默认值"之后，原位置的值是什么 | | | 同上 | | |
| 6 | C-02 | 对一个带销毁行为的值调用 `mem::forget` 后，销毁计数增加多少 | | | 同上 | | |
| 7 | C-03 | 同一作用域对同一值取两次可变借用，编译器报的错误码是什么 | | | `cargo test -p m1-ownership --test c03_borrow` | | |
| 8 | C-03 | 在已持有可变借用时再借一次的**运行期**版本，会发生什么（返回错误/panic/正常） | | | 同上 | | |
| 9 | C-04 | 悬垂引用（引用活得比被引用数据久）的错误码是什么 | | | `cargo test -p m1-ownership --test c04_lifetime` | | |
| 10 | C-04 | 只含一个不占空间的类型参数标记 + 一个 `u32` 字段的结构体，`size_of` 是多少 | | | 同上 | | |
| 11 | C-03 | 可变借用在**最后一次使用之后**、作用域结束**之前**，能否再取不可变借用 | | | 同上 | | |
| 12 | C-01 | MIR 里销毁的调用是源码里写过的吗；一个作用域里出现几次 | | | `tools/emit-mir.sh m1-ownership --example c01_ownership` | | |

## 未命中复盘                   <!-- 仅当存在 `一致? = ✗` -->

每条不一致写三段。不要只写"记错了"——要定位到心智模型的哪一步出了偏差。

### #N（C-xx）

- **我原本以为**：
- **实际是**：
- **我的心智模型错在哪一步**：

## 打开 Answer Track 的记录       <!-- 仅当翻过答案 -->

| 时间 | 翻的是哪份 | 触发条件（§H4.1 的第几条） | 当时的卡点 |
|------|-----------|------------------------|-----------|
| | | | |
