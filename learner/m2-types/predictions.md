# Learner Track — Module 2 预测表

**Story**: US2 | **Capabilities**: C-05…C-07

## 规则

1. `我的预测` 与 `依据` 两列 MUST 在运行**任何**验证命令之前填写并**提交**。
2. 提交之后才允许跑 `验证命令`，然后回填 `实测` 与 `一致?`。
3. 事后回填的预测**无效**，该行判未完成。
4. `依据` 写不出来就填"暂无依据" —— 那同样是有效信息，说明这一项你还没形成判断。
   写"感觉是"或"应该吧"则等于没填。

> `一致? = ✗` **不是失败**。它精确定位了一处心智模型缺口，是本模块最有价值的产出。
> 反过来，11 项全中才值得怀疑 —— 检查一下是不是事后回填的。

## 预测表

| # | C-ID | 预测项 | 我的预测 | 依据（为什么） | 验证命令 | 实测 | 一致? |
|---|------|-------|---------|--------------|---------|------|-------|
| 1 | C-05 | `#[repr(C)]` 的包头结构（`u8` + `u8` + 2 字节填充 + `u32`）的 `size_of` 是多少 | | | `cargo test -p m2-types --test c05_layout` | | |
| 2 | C-05 | 同一个结构里，`u32` 字段相对起始地址的 `offset_of` 是多少 | | | 同上 | | |
| 3 | C-05 | `Option<&u8>` 与 `&u8` 是否同宽 | | | 同上 | | |
| 4 | C-05 | `Option<u8>` 与 `u8` 是否同宽 | | | 同上 | | |
| 5 | C-05 | 一个带 `u32` 最大载荷的四变体状态机 enum，其 `size_of` 是否**严格等于** `size_of::<u32>()`（即"只有载荷、没有判别式空间"） | | | 同上 | | |
| 6 | C-06 | 指向 trait 对象的引用，宽度是几个 `usize` | | | `cargo test -p m2-types --test c06_trait` | | |
| 7 | C-06 | 指向具体类型的引用，和指向 trait 对象的引用，谁更宽（或一样宽） | | | 同上 | | |
| 8 | C-06 | 同一输入分别走静态分发与动态分发，返回值是否相等 | | | 同上 | | |
| 9 | C-07 | 泛型函数体里用了 `>` 但类型参数没有比较 bound，编译器报的错误码是什么 | | | `cargo test -p m2-types --test c07_generic` | | |
| 10 | C-07 | 同一套比较逻辑的泛型版本与 trait 对象版本，对相同的一对输入，返回值是否相等 | | | 同上 | | |
| 11 | C-07 | 一份泛型函数被 `u8` / `u16` / `u32` 各调用一次，nightly 打印出的单态化实例数是几 | | | 见下方 nightly 命令 | | |

### #11 的观察命令（先填预测，再跑）

```bash
# 在仓库根目录。产物是编译器 stderr 上的实例清单，属于 NON-ASSERTION。
cargo +nightly rustc -p m2-types --example c07_generic -- -Z print-mono-items=yes
```

数的时候只数**你写的那份泛型函数**被具体类型实例化的次数，不要把标准库的实例算进去。

## 未命中复盘                   <!-- 仅当存在 `一致? = ✗` -->

每条不一致写三段。不要只写"记错了"——要定位到心智模型的哪一步出了偏差。

### #N（C-xx）

- **我原本以为**：
- **实际是**：
- **我的心智模型错在哪一步**：

## 打开 Answer Track 的记录       <!-- 仅当翻过答案 -->

| 时间 | 翻的是哪份 | 触发条件（§H4.1 的第几条） | 当时的卡点 |
|------|-----------|------------------------|-----------|
| | | | |
