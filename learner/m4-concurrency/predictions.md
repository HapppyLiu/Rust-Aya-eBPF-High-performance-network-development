# Learner Track — Module 4 预测表

**Story**: US4 | **Capabilities**: C-12…C-14

## 规则

1. `我的预测` 与 `依据` 两列 MUST 在运行**任何**验证命令之前填写并**提交**。
2. 提交之后才允许跑 `验证命令`，然后回填 `实测` 与 `一致?`。
3. 事后回填的预测**无效**，该行判未完成。
4. `依据` 写不出来就填"暂无依据" —— 那同样是有效信息，说明这一项你还没形成判断。
   写"感觉是"或"应该吧"则等于没填。

> `一致? = ✗` **不是失败**。它精确定位了一处心智模型缺口，是本模块最有价值的产出。
> 反过来，11 项全中才值得怀疑 —— 检查一下是不是事后回填的。
>
> 题集 `acceptance/send-sync-quiz.md` 的作答**不在本表**。本表只预测实验；
> 题集在学完后一次性写入 `send-sync-quiz.result.md`。

## 预测表

| # | C-ID | 预测项 | 我的预测 | 依据（为什么） | 验证命令 | 实测 | 一致? |
|---|------|-------|---------|--------------|---------|------|-------|
| 1 | C-12 | `Cell<u32>` 是否满足"可把值移动到另一线程"这条标记 | | | `cargo test -p m4-concurrency --test c12_send_sync` | | |
| 2 | C-12 | `Cell<u32>` 是否满足"可把共享引用送到另一线程"这条标记 | | | 同上 | | |
| 3 | C-12 | `Mutex<Cell<u32>>` 是否同时满足上面两条标记 | | | 同上 | | |
| 4 | C-12 | 在 `thread::scope` 里让两个闭包都碰到同一个 `Cell`，编译器报的错误码是什么 | | | 同上（`compile_fail/c12_cell_across_threads.rs`） | | |
| 5 | C-13 | 两个 `scope` 线程各自对同一局部整数做 `+=`（不加锁），编译器报的错误码是什么 | | | `cargo test -p m4-concurrency --test c13_concurrency` | | |
| 6 | C-13 | 4 个线程经互斥锁各加 25，总和是多少（先填数字，再让测试核对） | | | 同上 | | |
| 7 | C-13 | example 打印的"谁先结束"能不能写成稳定断言（能 / 不能） | | | `cargo run -p m4-concurrency --example c13_concurrency` | | |
| 8 | C-14 | 4 个线程、每个 `SeqCst` 自增 25 次，原子计数器的终值是多少 | | | `cargo test -p m4-concurrency --test c14_atomic` | | |
| 9 | C-14 | 释放/获取握手：读线程看见旗标之后，载荷应是多少 | | | 同上 | | |
| 10 | C-14 | 两边都改成最弱内存序之后，哪些执行顺序从"不可能"变成"可能" | | | `cargo run -p m4-concurrency --example c14_atomic` | | |
| 11 | C-14 | 本机 x86_64 上，#10 的"可能"是否一定会出现在这一次打印里（一定出现 / 不一定） | | | 同上；架构相关性见跑完后的 OBSERVATIONS | | |

## 未命中复盘                   <!-- 仅当存在 `一致? = ✗` -->

每条不一致写三段。不要只写"记错了"——要定位到心智模型的哪一步出了偏差。

### #N（C-xx）

- **我原本以为**：
- **实际是**：
- **我的心智模型错在哪一步**：

## 打开 Answer Track 的记录       <!-- 仅当翻过答案 -->

| 时间 | 翻的是哪份 | 触发条件（§H4.1 的第几条） | 当时的卡点 |
|------|-----------|------------------------|-----------|
| | | | |
