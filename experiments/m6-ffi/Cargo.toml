[package]
name = "m6-ffi"
edition.workspace = true
version.workspace = true
publish.workspace = true
description = "US6 / C-21：FFI、repr(C) 布局、跨边界所有权、Linux errno 封装"

# R-08：本 Feature 唯一允许的外部依赖，且仅限本 crate。
[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"

[lints]
workspace = true
