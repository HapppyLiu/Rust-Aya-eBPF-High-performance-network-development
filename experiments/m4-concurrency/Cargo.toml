[package]
name = "m4-concurrency"
edition.workspace = true
version.workspace = true
publish.workspace = true
description = "US4 / C-12..C-14：Send/Sync、线程与互斥、原子操作与内存序"

[dependencies]

# harness 只在验收侧使用：examples/ 负责观察（不依赖 harness），
# tests/ 负责断言（用 harness 做错误码断言）。
[dev-dependencies]
rf-harness.workspace = true

[lints]
workspace = true
