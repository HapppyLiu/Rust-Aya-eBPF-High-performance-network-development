# C-03 Borrowing — Acceptance Criterion

**Module**: m1 | **Story**: US1 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m1-ownership --test c03_borrow
```

## 通过判据

- [x] 上述命令退出码为 0（7 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测两个 `compile_fail` 样本的错误码，
      预测与实际一致：`c03_two_mut_borrows.rs` → E0499、
      `c03_mut_while_shared.rs` → E0502
- [x] 能把同一条借用规则的**两种执行时机**并排说明，且每一侧都有实验支撑：
      编译期（borrow checker → 错误码）与运行期（`RefCell` → panic）
- [x] 能定位运行期版本的**全部状态**并说明其编码：`BorrowCounter = isize`
      （`core/src/cell.rs:945`），`0` = 无借用 / 正数 = 读借用计数 / 负数 = 写借用
- [x] 能说明该 `isize` 与编译期借用检查的全函数控制流分析在代价上的差别，
      并指出这笔交换换来什么、付出什么（错误从编译期推迟到运行期）
- [x] 能解释 NLL 使借用在**最后一次使用**后结束，而非作用域末尾，
      并说明 `nll_ends_borrow_at_last_use` **能编译**这一事实本身即为证明
- [x] 能证明 `RefCell` 并未放宽规则、只改变了检查时机：
      `sequential_borrows_are_legal_in_both_regimes` 显示合法的顺序访问在两侧都通过
- [x] 能指出 `borrow_mut` 二次借用报 `already borrowed`、
      而 `borrow` 遇既存可变借用报 `already mutably borrowed`，
      并说明为何断言只匹配子串而非全文（诊断措辞属非断言产物）
- [x] 能说明借用检查器是**保守**的：它拒绝"无法证明安全"的代码，
      而非"已证明不安全"的代码，并指出这与 eBPF verifier 的失败模式同源
- [x] 源码引用已记录：`core/src/cell.rs:849`（`RefCell` 定义）、
      `:945`（`BorrowCounter`）、`:946/949/954`（`UNUSED`/`is_writing`/`is_reading`）、
      `:924`（`panic_already_mutably_borrowed`）
- [x] 编译期借用检查一项按 §B2 记为 **reference-fallback**（`rustc_borrowck`
      属编译器而非 `library/`），且已书面说明它与 `RefCell` 是同一规则的两种时机、
      不能互相替代
- [x] `UB Tool = n/a`（本模块无 unsafe 代码），故 `ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）。
      `RefCell` 借用冲突以 panic 收场，panic 不是 UB

## 结果

**Status**: pass

**评估日期**：2026-09-04
**环境记录**：[../../experiments/m1-ownership/OBSERVATIONS.md](../../experiments/m1-ownership/OBSERVATIONS.md)
