# C-16 Raw pointer — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m5-unsafe --test c16_raw_ptr
cargo test -p m5-unsafe --test c16_raw_ptr_ub
cargo +nightly miri test -p m5-unsafe --test c16_raw_ptr
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c16_raw_ptr` 3 个测试、
      `c16_raw_ptr_ub` 1 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 写再读圆回写入的值（`write_then_read_roundtrips`）
- [x] 两次 `addr_of!` 是同一位置，不断言具体地址数值
      （`two_addr_of_are_the_same_place`）
- [x] 对照侧 `ub_verdict` 事前预测 **W1 + W6**，实际命中 `has been freed`；
      安全侧 **clean**
- [x] 每个 unsafe 块 SAFETY 覆盖五要素
- [x] 源码引用已记录：`core/src/ptr/mod.rs:1692/1916/2729`；`const_ptr.rs:1148`

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
