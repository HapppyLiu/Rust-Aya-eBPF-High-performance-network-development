# C-06 Trait — Acceptance Criterion

**Module**: m2 | **Story**: US2 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m2-types --test c06_trait
```

## 通过判据

- [x] 上述命令退出码为 0（3 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能标注同一 trait 的静态分发与动态分发调用点，
      并说明实现分别在**单态化时**与**每次调用从 vtable load** 时选定（US2 AS1）
- [x] 能用 `size_of` 证明 `&dyn Score` 宽两个 `usize`、`&Ping` 宽一个 `usize`
      （`dyn_trait_reference_is_two_usizes`）
- [x] 能指出两种分发对同一输入返回值相等，且**不能**由返回值推出代价相等
      （`static_and_dynamic_dispatch_agree_on_results` +
      OBSERVATIONS「C-06：直接调用 vs vtable」）
- [x] 能在 LLVM IR 中定位：`score_static::<Ping>` 是带一个数据指针的直接 `call`，
      `score_dynamic` 带数据指针 + `@vtable.N`；
      `score_dynamic` 函数体是从 vtable load 之后的间接 `call`
- [x] 能说明 `{}` 解析到 `Display::fmt`、`label.len()` 沿 `Deref::Target = str` 继续查找，
      且两者都不创造第三种分发（`display_and_deref_resolve_to_the_impls`）
- [x] 源码引用已记录：`core/src/fmt/mod.rs:1187/1212`（`Display` / `fmt`）、
      `core/src/ops/deref.rs:139/150`（`Deref` / `deref`）；
      分发方式一项按 §B2 记为 **reference-fallback**（代码生成，无库代码承载）
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-06
**环境记录**：[../../experiments/m2-types/OBSERVATIONS.md](../../experiments/m2-types/OBSERVATIONS.md)
