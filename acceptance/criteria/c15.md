# C-15 Unsafe Rust — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m5-unsafe --test c15_unsafe
cargo test -p m5-unsafe --test c15_unsafe_ub
cargo +nightly miri test -p m5-unsafe --test c15_unsafe
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c15_unsafe` 4 个测试、
      `c15_unsafe_ub` 1 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 边界内 `get_unchecked` 读到的值与直接索引相同
      （`get_unchecked_in_bounds_matches_index`）
- [x] 越界下标被安全路径拦下，返回 `None`（`out_of_bounds_is_none`）
- [x] 对照侧 `ub_verdict` 事前预测为 **W1 + W2**，实际命中
      （`` `assume` called with `false` ``，W2 在本工具链上的别名）；
      安全侧事前预测 **clean**，`cargo +nightly miri test --test c15_unsafe` 退出码 0
- [x] 每个 unsafe 块 SAFETY 覆盖五要素（见 `acceptance/safety-invariant-audit.md`）
- [x] 源码引用已记录：`core/src/slice/mod.rs:612-619/640`；UB 定义记 reference-fallback

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
