# C-05 Struct / Enum — Acceptance Criterion

**Module**: m2 | **Story**: US2 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m2-types --test c05_layout
```

## 通过判据

- [x] 上述命令退出码为 0（5 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下画出 `#[repr(C)]` 包头的字节图，
      并预测 `size_of` / `align_of` / `offset_of!(…, length)`，
      预测与 `repr_c_header_has_deterministic_layout` 一致
- [x] 能说明字段顺序改变的是**对齐空位**而不是有效载荷，
      并用 `field_order_changes_padding_not_payload` 的偏移数字指出空位在哪
- [x] 能解释 enum 状态机同时放下判别式与变体载荷，
      并指出 `ConnState` 的大小**严格大于**最大载荷 `u32`，
      而无载荷的 `WireKind` 大小等于判别式本身
      （`enum_occupies_discriminant_plus_payload_space`）
- [x] 能说明为何该布局在无 GC 环境下可用：判别式是编译期排好的整数，
      不需要运行期对象头或类型信息（US2 AS2）
- [x] 能区分两类 `Option<T>`：`Option<&u8>` 与 `&u8` 同宽（文档化 NPO），
      `Option<u8>` 必须另辟判别式（`option_ref_has_the_same_width_as_the_reference` /
      `option_u8_needs_an_extra_discriminant_byte`）
- [x] 源码引用已记录：`core/src/option.rs:118-153`（NPO 保证与名单）、
      `:598`（`Option` 定义）、`core/src/mem/mod.rs:373/540/1617`
      （`size_of` / `align_of` / `offset_of`）
- [x] `UB Tool = n/a`（本模块无 unsafe 代码），故 `ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-06
**环境记录**：[../../experiments/m2-types/OBSERVATIONS.md](../../experiments/m2-types/OBSERVATIONS.md)
