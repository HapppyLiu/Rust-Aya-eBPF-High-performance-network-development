# C-13 Concurrency — Acceptance Criterion

**Module**: m4 | **Story**: US4 | **UB tool**: miri（many-seeds）

## 验证命令

```bash
cargo test -p m4-concurrency --test c13_concurrency
tools/run-miri.sh m4-concurrency --many-seeds
```

## 通过判据

- [x] 上述 `cargo test` 命令退出码为 0（3 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c13_data_race.rs`
      的错误码，预测与实际一致：E0499（实际错误码集合仅此一个）
- [x] 能定位安全 Rust 中数据竞争被拒绝的具体规则（US4 AS3）：
      两个 scoped 线程对同一局部整数 `+=`，拒绝的是借用规则（E0499），
      不是"线程 API 忘了加锁"（`data_race_in_safe_rust_is_e0499`）
- [x] 不变量与完成顺序无关：`scoped_mutex_sum(4, 25) == 100`
      （`scoped_mutex_sum_is_order_independent`）；
      编号集合为 `{0,1,2,3}`（`scoped_id_set_is_complete`），**不断言排列**
- [x] 源码引用已记录：`std/src/thread/functions.rs:125/128`；
      `std/src/thread/scoped.rs:141/201`；
      `std/src/sync/poison/mutex.rs:227/238/257/289/294`
- [x] `ub_verdict` 事前预测为 **`clean`**；未跑 Miri 时记 `n/a`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m4-concurrency/OBSERVATIONS.md](../../experiments/m4-concurrency/OBSERVATIONS.md)
