# C-23 core / alloc / std — Acceptance Criterion

**Module**: m7 | **Story**: US7 | **UB tool**: compile-time

## 验证命令

```bash
cd experiments/m7-nostd && cargo build
tools/m7-probe-errors.sh 2a 2b
```

## 通过判据

- [x] 默认构建退出码 0：同一段求和的 **core 版与 alloc 版**均链接成功
      （alloc 版依赖本 crate 的 bump，不依赖 std） ← **由退出码决定（§D2）**
- [x] `tools/m7-probe-errors.sh 2a` 命中 E0433 `cannot find fs in core`；
      归属 **OS services**（`File` 不是 core 漏写的 API）
- [x] `tools/m7-probe-errors.sh 2b` 命中 E0463 `can't find crate for std`；
      归属 **OS services**；级联的 prelude / panic_handler **不另计分母**（CHK020 已列出全集）
- [x] 笼统归因"不能用标准库"判为未通过 —— 本 AC 要求能区分 alloc 再导出与 OS services
- [x] 源码引用已记录：`alloc/src/lib.rs:6-9,74-75`；`std/src/lib.rs:464,607`

## 结果

**Status**: pass

**评估日期**：2026-09-11
**环境记录**：[../../experiments/m7-nostd/OBSERVATIONS.md](../../experiments/m7-nostd/OBSERVATIONS.md)
