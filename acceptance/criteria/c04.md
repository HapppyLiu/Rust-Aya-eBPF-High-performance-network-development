# C-04 Lifetime — Acceptance Criterion

**Module**: m1 | **Story**: US1 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m1-ownership --test c04_lifetime
```

## 通过判据

- [x] 上述命令退出码为 0（9 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测三个 `compile_fail` 样本的错误码，
      预测与实际一致：`c04_missing_lifetime.rs` → E0106、
      `c04_dangling_ref.rs` → E0597、`c04_phantom_type_mismatch.rs` → E0308
- [x] 能复述三条省略规则，并用它们**逐条**解释 `longest` 为何编译不过
      （规则 2 因两个入参失效、规则 3 因无 `self` 失效）
      而 `first_word` 为何不需要标注（规则 2 直接给出答案）
- [x] 能说明编译器在两个候选来源之间"不猜"是**设计选择**而非能力不足，
      并给出理由（猜错的后果是悬垂引用，且无可靠依据可分辨）
- [x] 能指出 `'a` 表达的是**关系**而非具体时长，
      并用 `longest_returns_the_longer_input` 的两次调用（参数顺序对调）证实
- [x] 能在 MIR 层定位生命周期擦除的证据：`elided` 与 `annotated` 的函数体
      逐字符相同，且 `annotated` 的 MIR 签名里 `'a` **已不存在**
      —— 见 OBSERVATIONS §C-04 IR 观察
- [x] 能说明擦除发生在借用检查**之后**，因而"擦除"不等于"标注无用"，
      并以 `c04_dangling_ref.rs`（E0597）作为标注起作用的证据
- [x] 能解释 `PhantomData<T>` 为何大小为 0：它是**无字段**的单元结构体
      （`core/src/marker.rs:811`），且该保证是**文档化**的（`:805`），故可作稳定断言
- [x] 能把"类型级区分"与"运行期空间占用"分离，且两侧各有证据：
      不可互换性由 E0308 断言（编译期），
      运行期无差别由 `differently_tagged_values_share_the_same_runtime_representation` 断言
- [x] 能指出 `PhantomData` 的零成本结论**不可**推广到所有 ZST
      （高对齐 ZST 仍会参与布局计算）
- [x] 源码引用已记录：`core/src/marker.rs:811`（`PhantomData` 定义）、
      `:805`（零大小的文档保证）
- [x] 生命周期省略规则一项按 §B2 记为 **reference-fallback**
      （省略是编译器推导算法，规则写在 Rust Reference，无库代码承载），
      且已书面说明该 fallback 是"确实不存在"而非"没找到"
- [x] `UB Tool = n/a`（本模块无 unsafe 代码），故 `ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-04
**环境记录**：[../../experiments/m1-ownership/OBSERVATIONS.md](../../experiments/m1-ownership/OBSERVATIONS.md)
