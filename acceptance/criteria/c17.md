# C-17 Pointer arithmetic — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m5-unsafe --test c17_ptr_arith
cargo test -p m5-unsafe --test c17_ptr_arith_ub
cargo +nightly miri test -p m5-unsafe --test c17_ptr_arith
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c17_ptr_arith` 6 个测试、
      `c17_ptr_arith_ub` 1 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 分配内 `add` 读到对应元素（`add_within_allocation_reads_element`）
- [x] 分配内走一步，`add` 与 `wrapping_add` 重合
      （`wrapping_add_agrees_inside_allocation`）；`offset` 与 `add` 重合
      （`offset_agrees_with_add`）
- [x] 走一步的字节差等于 `size_of::<u32>()`（`one_step_is_one_element_wide`）
- [x] 对照侧 `ub_verdict` 事前预测 **W1 + W7**，实际命中
      `in-bounds pointer arithmetic failed`（W7 别名）；安全侧 **clean**
- [x] 每个 unsafe 块 SAFETY 覆盖五要素
- [x] 源码引用已记录：`const_ptr.rs:354/838/985-988/1035`

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
