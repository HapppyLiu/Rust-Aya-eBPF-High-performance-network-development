# C-12 Send / Sync — Acceptance Criterion

**Module**: m4 | **Story**: US4 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m4-concurrency --test c12_send_sync
cargo test -p m4-concurrency --test c12_send_sync_quiz
tools/run-miri.sh m4-concurrency --many-seeds
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c12_send_sync` 7 个测试、
      `c12_send_sync_quiz` 8 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c12_cell_across_threads.rs`
      的错误码，预测与实际一致：E0277（实际错误码集合仅此一个）
- [x] 能分别陈述 `Send` 与 `Sync` 各自约束什么，而不把两者混为一谈（US4 AS1）：
      `Cell<u32>` 是 `Send` 不是 `Sync`（`cell_is_send_not_asserted_sync` +
      `moving_cell_preserves_value`）；
      `Mutex<Cell<u32>>` 两者皆是（`mutex_of_cell_is_send_and_sync`）
- [x] 题集客观校验命令退出码为 0：
      `cargo test -p m4-concurrency --test c12_send_sync_quiz`
      （正向 `assert_send`/`assert_sync` + 9 个 `quiz_*.rs` 的 E0277）
- [x] 一次性作答错 ≤ 1 题且每题有推导依据：
      [`send-sync-quiz.result.md`](../send-sync-quiz.result.md) 错题数 = 0
- [x] 源码引用已记录：`core/src/marker.rs:92/97/99/105/657/672/674`；
      `core/src/cell.rs:317/325/2328`
- [x] `ub_verdict` 事前预测为 **`clean`**（本能力无故意 UB）；
      未跑 Miri 时记 `n/a`，不得记 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m4-concurrency/OBSERVATIONS.md](../../experiments/m4-concurrency/OBSERVATIONS.md)
