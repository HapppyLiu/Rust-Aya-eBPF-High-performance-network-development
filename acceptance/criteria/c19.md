# C-19 Aliasing — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri（SB + TB 对照）

## 验证命令

```bash
cargo test -p m5-unsafe --test c19_aliasing
cargo test -p m5-unsafe --test c19_aliasing_ub
cargo +nightly miri test -p m5-unsafe --test c19_aliasing
MIRIFLAGS="-Zmiri-tree-borrows" cargo +nightly miri test -p m5-unsafe --test c19_aliasing
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c19_aliasing` 2 个测试、
      `c19_aliasing_ub` 2 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] `UnsafeCell` 顺序写两次读回最后一次（`unsafecell_sequential_writes`）
- [x] 对照侧两轮**各一条**断言，不断言两轮必须相同（plan.md 规则 3）：
      SB 命中 **W1 + W8**（`overlapping_mut_is_ub_under_stacked_borrows`）；
      TB 命中 **W1 + W9**（`overlapping_mut_is_ub_under_tree_borrows`，本工具链为 `foreign write`）
- [x] 两套都报 UB → 情形 1，`ub_verdict = expected-ub`，非模型敏感
- [x] 安全侧两轮 Miri `clean`
- [x] 每个 unsafe 块 SAFETY 覆盖五要素
- [x] 源码引用已记录：`core/src/cell.rs:2323/2328/2443`

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
