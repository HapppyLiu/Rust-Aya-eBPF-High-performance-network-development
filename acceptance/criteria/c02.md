# C-02 Move semantics — Acceptance Criterion

**Module**: m1 | **Story**: US1 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m1-ownership --test c02_move
```

## 通过判据

- [x] 上述命令退出码为 0（10 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测两个 `compile_fail` 样本的错误码，
      预测与实际一致：`c02_copy_with_drop.rs` → E0184、
      `c02_move_out_of_borrow.rs` → E0507
- [x] 能用**销毁计数**（而非语言描述）区分移动与复制：
      链式移动后计数为 1（`move_does_not_duplicate_drops`），
      `Clone` 后为 2（`clone_creates_an_independent_value`），
      `mem::forget` 后为 0（`forget_suppresses_drop`）
- [x] 能解释 `Copy` 与 `Drop` 为何**必须**互斥，且理由落到 double free
      而非"语言规定"，并能指出标准库把该理由写在何处（`core/src/marker.rs:419`）
- [x] 能说明 `replace` 无额外约束而 `take` 要求 `T: Default` 的原因，
      并用实验证实两者都不留下无效状态
      （`neither_replace_nor_take_leaves_an_invalid_state`）
- [x] 能解释 `mem::forget` 为何是**安全**函数，并把"泄漏"与"UB"区分为两个范畴
- [x] 能在 MIR 层定位"移动只是簿记"的证据：移动源 local 的 `drop` terminator
      数量为 **0**，目标 local 为 2（正常路径 + unwind 路径）
      —— 见 OBSERVATIONS §C-02 IR 观察的计数校验
- [x] 源码引用已记录（路径 + 符号 + 实际行号 + "这段源码回答了什么"）：
      `core/src/mem/mod.rs:953`（`replace`）、`:886`（`take`）、
      `:189`（`forget`）、`:822`（`swap`）
- [x] `UB Tool = n/a`（本模块无 unsafe 代码），故 `ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）。
      注意 `mem::forget` 会造成资源泄漏，但泄漏不属于 UB，
      因此本条**不**因使用 `forget` 而需要 Miri

## 结果

**Status**: pass

**评估日期**：2026-09-04
**环境记录**：[../../experiments/m1-ownership/OBSERVATIONS.md](../../experiments/m1-ownership/OBSERVATIONS.md)
