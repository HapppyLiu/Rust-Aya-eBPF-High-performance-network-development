# C-01 Ownership — Acceptance Criterion

**Module**: m1 | **Story**: US1 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m1-ownership --test c01_ownership
```

## 通过判据

- [x] 上述命令退出码为 0（7 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测两个 `compile_fail` 样本的错误码，
      预测（样本首行 `//! EXPECT:`）与 `rf_harness` 提取的实际错误码一致：
      `c01_use_after_move.rs` → E0382、`c01_manual_drop_call.rs` → E0040
- [x] 能预测并断言销毁**顺序**，而非仅"销毁发生了"：
      `drop_order_is_reverse_of_declaration` 断言序列为 `["third","second","first"]`
- [x] 能说明销毁**代码**的来源与销毁**时刻**的决定因素是两件不同的事，
      并各自给出证据：代码来自编译器生成的 drop glue（MIR 中的 `drop` terminator），
      时刻由静态作用域决定（OBSERVATIONS §C-01 IR 观察）
- [x] 能解释所有权转移后销毁点为何随之转移，并用实验证实：
      `ownership_transfer_moves_drop_responsibility` 显示销毁发生在 `consume` 内部
- [x] 能定位 drop glue 在 MIR 中的插入位置，并解释 unwind 清理链
      为何按构造进度分段入口（`bb91`/`bb92`/`bb93` 各自的入口点不同）
- [x] 源码引用已记录（路径 + 符号 + 实际行号 + "这段源码回答了什么"）：
      `core/src/ops/drop.rs:209`（`Drop` trait）、`:206`（`#[lang = "drop"]`）、
      `core/src/marker.rs:454`（`Copy`）、`:419`（互斥理由原文）
      —— 见 [learning/m1-ownership/source-refs.md](../../learning/m1-ownership/source-refs.md)
- [x] `UB Tool = n/a`（本模块无 unsafe 代码），故 `ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-04
**环境记录**：[../../experiments/m1-ownership/OBSERVATIONS.md](../../experiments/m1-ownership/OBSERVATIONS.md)
