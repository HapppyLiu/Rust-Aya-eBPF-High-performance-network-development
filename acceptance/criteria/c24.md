# C-24 Panic and allocator fundamentals — Acceptance Criterion

**Module**: m7 | **Story**: US7 | **UB tool**: compile-time + Miri（host 侧 allocator）

## 验证命令

```bash
cd experiments/m7-nostd && cargo build
tools/m7-probe-errors.sh 3a 3b
cargo test -p rf-harness --test c24_bump_alloc
cargo +nightly miri test -p rf-harness --test c24_bump_alloc
```

## 通过判据

- [x] 默认构建退出码 0：`Vec::push` 在已注册 bump `#[global_allocator]` 的前提下链接成功
      ← **由退出码决定（§D2）**
- [x] `tools/m7-probe-errors.sh 3a` 命中 E0433 crate `alloc`；归属 **alloc**
- [x] `tools/m7-probe-errors.sh 3b` 命中 `no global memory allocator found`；归属 **allocator**
      （1.98.0 不再强制 `alloc_error_handler`）
- [x] 每条错误的归属正确率 100%（与 C-22/C-23 合计分母 6）；
      "'不能用标准库'式笼统归因"未出现
- [x] host 侧 `cargo test -p rf-harness --test c24_bump_alloc` 3 项通过
- [x] `cargo +nightly miri test -p rf-harness --test c24_bump_alloc` 退出码 0；
      `ub_verdict = clean`（**仅** host 侧 bump 方法；MUST NOT 写成"裸机产物无 UB"）
- [x] `BumpAlloc` 的 `unsafe impl GlobalAlloc` / `Sync` SAFETY 覆盖五要素
- [x] 说明"分配能力由 BumpAlloc 提供"以及该前提在 eBPF 环境不成立
      （`learning/m7-nostd/concept.md` / OBSERVATIONS「C-24 / Vec 链接成功」）
- [x] 源码引用已记录：`core/src/panicking.rs:10-22,67-69`；
      `core/src/alloc/global.rs:144,178`；`alloc/src/alloc.rs:12-22,530-532`

## 结果

**Status**: pass

**评估日期**：2026-09-11
**环境记录**：[../../experiments/m7-nostd/OBSERVATIONS.md](../../experiments/m7-nostd/OBSERVATIONS.md)
