# C-11 Smart pointer — Acceptance Criterion

**Module**: m3 | **Story**: US3 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m3-composition --test c11_smart_ptr
```

## 通过判据

- [x] 上述命令退出码为 0（5 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c11_rc_across_threads.rs`
      的错误码，预测与实际一致：E0277（实际错误码集合仅此一个）
- [x] 能说明选择依据是所有权语义（US3 AS3）：
      `Box` 独占（`box_is_unique_owner`）；
      `Rc` 单线程共享，`clone` 加计数（`rc_clone_shares_and_bumps_strong_count`）；
      `Arc` 可跨线程共享（`arc_clone_shares_and_is_send`）
- [x] 能指出误用后果：`Rc` 即使 `move` 进 `spawn` 也被拒（`rc_across_threads_is_e0277`）
- [x] 能用分配次数证明 `Rc::clone` / `Arc::clone` 不新分配：
      `Box::new`、`Rc::new`+`clone`、`Arc::new`+`clone` 各 `allocs = 1`
      （`smart_pointer_allocation_counts`）
- [x] 源码引用已记录：`alloc/src/boxed.rs:234`；
      `alloc/src/rc.rs:320/330/338/1814/2495`；
      `alloc/src/sync.rs:269/279/281/2383`
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-07
**环境记录**：[../../experiments/m3-composition/OBSERVATIONS.md](../../experiments/m3-composition/OBSERVATIONS.md)
