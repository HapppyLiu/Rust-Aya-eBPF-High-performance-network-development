# C-08 Error handling — Acceptance Criterion

**Module**: m3 | **Story**: US3 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m3-composition --test c08_error
```

## 通过判据

- [x] 上述命令退出码为 0（2 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c08_missing_from.rs` 的错误码，
      预测与实际一致：E0277（实际错误码集合仅此一个）
- [x] 能说明 `?` 失败路径调用 `From::from`（`result.rs:2192`），
      缺 `From` 的拒绝发生在单态化**之前**
- [x] 能指出同一组输入上，手写 `match` 传播与 `?` 传播的 `Ok`/`Err` 一致
      （`manual_and_question_mark_agree_on_every_path`）
- [x] 能区分两类失败：内层 `ParseErr` 经 `From` 变成 `AppErr::Parse`，
      本层 `OutOfRange` 不再经过 `From`
- [x] 源码引用已记录：`core/src/result.rs:557/2185/2192`、
      `core/src/convert/mod.rs:589/594`、`std/src/error.rs:4`、`core/src/error.rs:59`
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-07
**环境记录**：[../../experiments/m3-composition/OBSERVATIONS.md](../../experiments/m3-composition/OBSERVATIONS.md)
