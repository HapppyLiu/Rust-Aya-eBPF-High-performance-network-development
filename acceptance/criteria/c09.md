# C-09 Iterator — Acceptance Criterion

**Module**: m3 | **Story**: US3 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m3-composition --test c09_iterator
```

## 通过判据

- [x] 上述命令退出码为 0（2 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测多阶段链的求值顺序：
      按元素走完整条链（`map` 与 `filter` 交错），而不是先耗尽第一段
      （OBSERVATIONS「C-09：副作用出现在消费时」）
- [x] 能预测并核对分配次数（US3 AS1）：
      `map`+`sum` 的 `allocs = 0`；精确 `size_hint` 的 `map`+`collect` 的 `allocs = 1`；
      只构造链不消费的 `allocs = 0`；空 `Vec`+`push` 的分配事件多于一次到位的 `collect`
      （`iterator_chain_allocation_counts`）
- [x] 能指出手写循环与迭代器链的**元素结果**一致，且这**不能**推出分配次数一致
      （`loop_and_iterator_agree_on_values`）
- [x] 源码引用已记录：`core/src/iter/traits/iterator.rs:42/78/831`、
      `core/src/iter/adapters/map.rs:61/106/111`
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-07
**环境记录**：[../../experiments/m3-composition/OBSERVATIONS.md](../../experiments/m3-composition/OBSERVATIONS.md)
