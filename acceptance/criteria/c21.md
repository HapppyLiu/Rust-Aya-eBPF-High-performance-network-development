# C-21 FFI — Acceptance Criterion

**Module**: m6 | **Story**: US6 | **UB tool**: asan

## 验证命令

```bash
cargo test -p m6-ffi --test c21_ffi_layout
cargo test -p m6-ffi --test c21_ffi
cargo test -p m6-ffi --test c21_ffi_ownership
cargo test -p m6-ffi --test c21_errno
cargo test -p m6-ffi
tools/run-asan.sh m6-ffi
```

## 通过判据

- [x] 上述 `cargo test -p m6-ffi` 退出码为 0（layout 2 + ffi 5 + ownership 2 + errno 4，
      共 13 个 integration test 全部通过） ← **这一条由退出码决定（§D2）**
- [x] `tools/run-asan.sh m6-ffi` 退出码为 0 ← **同样由退出码决定（§D2）**
- [x] 双向调用各自成立（CHK042）：`rust_to_c_add_returns_sum`（Rust→C）与
      `c_to_rust_mul_returns_product`（C→Rust）分开通过，不能互相顶替
- [x] 双侧布局断言（CHK043）：`repr_c_layout_matches_c_compiler` 对
      `size` / `align` / 三个 `offset` 逐项相等；MUST NOT 以"声明了 `repr(C)`"代替测量
- [x] 释放责任（CHK044 / US6 AS2）：`c_alloc_rust_free_roundtrips_literal` 断言
      C `malloc` 的内容由 Rust `free` 后读回约定字面量；约定不一致的故障形态已写入
      `learning/m6-ffi/concept.md` 与 `OBSERVATIONS.md`「约定不一致的故障形态」
- [x] errno 封装未丢失原始码（US6 AS3）：`open_missing_preserves_enoent` 与
      `raw_os_error_roundtrips_through_io_error`；concept.md 写明封装层假设
- [x] `ub_verdict` 实际值与事前预期一致：ASan 覆盖面内 **clean**；
      表述为「ASan 未在本次运行中观测到内存错误」，MUST NOT 表述为「无 UB」（CHK045）
- [x] 每个 unsafe 块 SAFETY 覆盖五要素；`m6_rust_hdr_sum` 另有 `# Safety` 文档段
- [x] 源码引用已记录：`core/src/ffi/mod.rs:36-38`；`core/src/ffi/primitives.rs:21,28,131-133,184`；
      `std/src/ffi/c_str.rs:10`；`core/src/ffi/c_str.rs:254`

## 结果

**Status**: pass

**评估日期**：2026-09-10
**环境记录**：[../../experiments/m6-ffi/OBSERVATIONS.md](../../experiments/m6-ffi/OBSERVATIONS.md)
