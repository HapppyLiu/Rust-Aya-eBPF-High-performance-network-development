# C-07 Generic — Acceptance Criterion

**Module**: m2 | **Story**: US2 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m2-types --test c07_generic
```

## 通过判据

- [x] 上述命令退出码为 0（5 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c07_missing_bound.rs` 的错误码，
      预测与实际一致：E0277
- [x] 能说明直接对无 bound 的 `T` 写 `>` 会先落成 E0369，
      本样本走"调用带 `PartialOrd` bound 的函数"才对准 E0277
- [x] 能说明缺 bound 的拒绝发生在单态化**之前**（候选选择失败，尚未复制函数体）
- [x] 能用 nightly `-Z print-mono-items=yes` 数出 `max_of` 对 `u8`/`u16`/`u32`
      各有一份实例（实例数 = 3），并说明标准库适配器实例不应计入
      （OBSERVATIONS「C-07：max_of 的三条单态化实例」）
- [x] 能指出单态化的布局后果：`Wrapper<T>` 大小随 `T` 变
      （`monomorphization_produces_type_dependent_layout`）
- [x] 能指出同一套比较的泛型版本与 trait 对象版本行为等价，
      并说明体积账单在单态化侧、间接跳转账单在动态分发侧
      （`generic_and_dyn_comparison_are_behaviorally_equivalent`，US2 AS1）
- [x] 能说明 `Iterator::Item` 是关联类型、`count_gt` 的 bound 约束的是元素
      （`iterator_bound_filters_by_partial_ord_on_item`）
- [x] 源码引用已记录：`core/src/iter/traits/iterator.rs:42`（`Iterator` / `Item`）、
      `core/src/cmp.rs:250/1366`（`PartialEq` / `PartialOrd`）；
      单态化一项按 §B2 记为 **reference-fallback**
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-06
**环境记录**：[../../experiments/m2-types/OBSERVATIONS.md](../../experiments/m2-types/OBSERVATIONS.md)
