# C-18 Alignment — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m5-unsafe --test c18_alignment
cargo test -p m5-unsafe --test c18_alignment_ub
cargo run -p m5-unsafe --example c18_alignment_ub
cargo +nightly miri run -p m5-unsafe --example c18_alignment_ub
cargo +nightly miri test -p m5-unsafe --test c18_alignment
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c18_alignment` 5 个测试、
      `c18_alignment_ub` 1 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] `(p as usize) % align_of::<u64>() == 0`（`local_u64_is_aligned`）
- [x] 对齐满足时普通 `read` 圆回（`aligned_read_roundtrips`）；
      `read_unaligned` 从偏移 1 读回本机字节序载荷
      （`read_unaligned_recovers_payload`）
- [x] 核心教学对照（T084 / US5 AS2）：普通 `cargo run --example c18_alignment_ub`
      退出码 0 并打印 `2`；同一源码 Miri 判定 UB，类别命中 **W1 + W2**
      （`memory access failed`）。打印 `2` 不得当作无 UB
- [x] 每个 unsafe 块 SAFETY 覆盖五要素
- [x] 源码引用已记录：`core/src/mem/mod.rs:540`；`core/src/ptr/mod.rs:1810`
- [x] 架构相关性已判定：打印 `2` **仅适用于 x86_64**（OBSERVATIONS C-18 UB）

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
