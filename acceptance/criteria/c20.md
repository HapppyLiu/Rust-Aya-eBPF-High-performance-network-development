# C-20 Memory safety — Acceptance Criterion

**Module**: m5 | **Story**: US5 | **UB tool**: miri

## 验证命令

```bash
cargo test -p m5-unsafe --test c20_mem_safety
cargo test -p m5-unsafe --test c20_mem_safety_ub
cargo run -p m5-unsafe --example c20_bounds_check
cargo +nightly miri test -p m5-unsafe --test c20_mem_safety
```

## 通过判据

- [x] 上述 `cargo test` 两条命令退出码为 0（`c20_mem_safety` 7 个测试、
      `c20_mem_safety_ub` 1 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 边界内解析与越界 `None`（`checked_parse_in_bounds` /
      `checked_parse_out_of_bounds_is_none`）
- [x] 先检查再 unchecked，结果相同（`unchecked_after_check_matches`）
- [x] `from_raw_parts` 包装在对齐偶数长度下给出 `len == 2`，奇数长度拒绝
      （`as_u16_slice_len` / `odd_len_rejected`）
- [x] 对照侧：对外未标 `unsafe` 的 `set_len` 封装，调用方只读 `v[0]` 即 UB；
      事前预测 **W1 + W10**，实际命中 `memory is uninitialized`
      （`lying_set_len_is_ub_under_miri`，US5 AS3 后半）
- [x] T090 / AS4：`c20_bounds_check` example 退出码 0；`concept.md` C-20 关联小节
      三项合取齐备；MUST NOT 编写 eBPF 程序（FR-017）
- [x] 每个 unsafe 块 SAFETY 覆盖五要素
- [x] 源码引用已记录：`core/src/slice/raw.rs:124`；`alloc/src/vec/mod.rs:2224`

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m5-unsafe/OBSERVATIONS.md](../../experiments/m5-unsafe/OBSERVATIONS.md)
