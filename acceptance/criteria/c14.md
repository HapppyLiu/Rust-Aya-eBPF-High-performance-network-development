# C-14 Atomic — Acceptance Criterion

**Module**: m4 | **Story**: US4 | **UB tool**: miri（many-seeds）

## 验证命令

```bash
cargo test -p m4-concurrency --test c14_atomic
tools/run-miri.sh m4-concurrency --many-seeds
```

## 通过判据

- [x] 上述 `cargo test` 命令退出码为 0（6 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能说明放宽内存序后哪些执行顺序变为可能（US4 AS2）：
      Release/Acquire 握手下看见旗标 ⇒ 载荷为 42
      （`release_acquire_handshake_sees_payload`）；
      两边 `Relaxed` 时语言允许"旗标已立、载荷仍是 0"，
      该窗口记在 OBSERVATIONS，**不断言**本机一定观察到旧值
- [x] `SeqCst` 下的确定性不变量：`seqcst_sum(4, 25) == 100`
      （`seqcst_increments_sum_to_product`）
- [x] `Ordering` 语义的可断言事实：`fetch_add` 返回旧值
      （`fetch_add_returns_previous`）；
      `Relaxed != SeqCst`（`ordering_variants_are_distinct`）；
      `size_of::<AtomicUsize>() == size_of::<usize>()`
      （`atomic_usize_has_usize_width`）
- [x] 源码引用已记录：`core/src/sync/atomic.rs:361/366/368/442/3825`
- [x] 本能力实验无 `unsafe` 块；`ub_verdict` 事前预测为 **`clean`**；
      未跑 Miri 时记 `n/a`（FR-019）
- [x] 架构相关性已判定：x86_64 TSO 会掩盖 Relaxed 握手窗口，
      该观察**仅适用于 x86_64**（FR-018 / OBSERVATIONS C-14）

## 结果

**Status**: pass

**评估日期**：2026-09-08
**环境记录**：[../../experiments/m4-concurrency/OBSERVATIONS.md](../../experiments/m4-concurrency/OBSERVATIONS.md)
