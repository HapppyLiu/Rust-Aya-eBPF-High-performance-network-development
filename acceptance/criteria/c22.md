# C-22 no_std — Acceptance Criterion

**Module**: m7 | **Story**: US7 | **UB tool**: compile-time + `nm`/`readelf`

## 验证命令

```bash
cd experiments/m7-nostd && cargo build
tools/check-nostd-artifact.sh
tools/m7-probe-errors.sh 1a 1b
```

## 通过判据

- [x] `cd experiments/m7-nostd && cargo build` 退出码为 0
      （"可运行"= 构建成功，CHK036） ← **由退出码决定（§D2）**
- [x] `tools/check-nostd-artifact.sh` 退出码为 0：有 `_start`、无 `__libc_start_main`、
      无 `eh_personality`、ELF64/X86-64 ← **由退出码决定（§D2）**
- [x] `tools/m7-probe-errors.sh 1a` 命中 `` `#[panic_handler]` function required ``；
      归属为 panic/runtime，**不是**"不能用标准库"
- [x] `tools/m7-probe-errors.sh 1b` 命中 `unwinding panics are not supported without std`；
      归属为 runtime（1.98.0 不单独索要 `eh_personality`，见 spec SC-006 修订）
- [x] 编译期断言：`build.rs` 锁定 `TARGET=x86_64-unknown-none`；
      `c22_nostd.rs` 在非 `none` 上 `compile_error!`，`PTR_WIDTH == 8`，`Option` 可用
- [x] 源码引用已记录：`core/src/lib.rs:12-14,37-44,64`；`std/src/lib.rs:236-237`

## 结果

**Status**: pass

**评估日期**：2026-09-11
**环境记录**：[../../experiments/m7-nostd/OBSERVATIONS.md](../../experiments/m7-nostd/OBSERVATIONS.md)
