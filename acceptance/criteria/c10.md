# C-10 Closure — Acceptance Criterion

**Module**: m3 | **Story**: US3 | **UB tool**: n/a（本模块无 unsafe 代码）

## 验证命令

```bash
cargo test -p m3-composition --test c10_closure
```

## 通过判据

- [x] 上述命令退出码为 0（4 个测试全部通过） ← **这一条由退出码决定（§D2）**
- [x] 能在**不运行程序**的前提下预测 `compile_fail/c10_borrow_escapes_thread.rs`
      的错误码，预测与实际一致：E0373（实际错误码集合仅此一个）
- [x] 能说明三种捕获分别满足哪一层调用 trait：
      共享引用 → `Fn`（`shared_ref_capture_is_fn`）；
      可变引用 → `FnMut`（`mut_ref_capture_is_fn_mut`）；
      按值拿走 `String` → `FnOnce`（`by_value_capture_is_fn_once`）
- [x] 能解释捕获方式如何决定闭包能否跨线程（US3 AS2）：
      按引用捕获局部变量缺的是 `'static`（E0373）；
      `move` 只解决这一问，捕获物还得是 `Send`（另一侧见 C-11）
- [x] 源码引用已记录：`core/src/ops/function.rs:76`（`Fn`）、`:163`（`FnMut`）、
      `:242`（`FnOnce`）；`spawn` 的 bound 在 `std/src/thread/functions.rs:128`
- [x] `UB Tool = n/a`，`ub_verdict` 记 **`n/a`** 而非 `clean`（FR-019）

## 结果

**Status**: pass

**评估日期**：2026-09-07
**环境记录**：[../../experiments/m3-composition/OBSERVATIONS.md](../../experiments/m3-composition/OBSERVATIONS.md)
